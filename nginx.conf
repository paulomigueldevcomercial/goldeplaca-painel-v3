server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index painel/index.html;

  location = / {
    return 302 /painel/;
  }

  location /painel/ {
    try_files $uri $uri/ /painel/index.html;
  }

  location ~* \.(?:js|css|ico|png|jpg|jpeg|gif|svg|woff2?)$ {
    expires 7d;
    add_header Cache-Control "public, max-age=604800, immutable";
    try_files $uri =404;
  }
}
